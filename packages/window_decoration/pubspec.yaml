name: window_decoration
description: "Fully custom windows and decorations for Flutter multi-window."
version: 0.2.0
homepage: https://github.com/rkishan516/window_decoration
publish_to: none

environment:
  sdk: ^3.12.0
  flutter: ">=3.42.0"

dependencies:
  flutter:
    sdk: flutter
  native_toolchain_ninja: ^0.1.1
  hooks: ^1.0.2
  code_assets: ^1.0.0
  logging: ^1.3.0
  ffi: ^2.2.0
  win32: ^6.0.0

dev_dependencies:
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
