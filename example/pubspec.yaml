name: window_decoration_example
description: Example app demonstrating the window_decoration package.
version: 0.2.0
publish_to: none

environment:
  sdk: ^3.12.0
  flutter: ">=3.42.0"

dependencies:
  flutter:
    sdk: flutter
  window_decoration:
    path: ../packages/window_decoration

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
